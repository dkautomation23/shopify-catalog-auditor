name: Feature request
description: Ask for something the tool does not do yet
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Check the README's "Honest limits" section first — image alt text and
        variant barcodes/GTINs are already a known, explained gap (the public
        catalog does not expose them). If your request is listed there, the
        answer, and the reason, is already written.
  - type: textarea
    id: problem
    attributes:
      label: What are you trying to get done?
      description: The situation, not the feature. The feature you are asking for may not be the right answer.
    validations:
      required: true
  - type: textarea
    id: today
    attributes:
      label: How do you do it today?
      description: What you run now, and where it falls short.
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Anything this should not break
